#include "scheduler.h"
#include <stdio.h>

int main()
{    
    // {id, arrival_time, burst_time, time_remaining, priority, timeline}
    Job jobs[3] = {
        {0, 0, 5, 0, 0, ""},
        {0, 0, 5, 0, 0, ""},
        {0, 0, 5, 0, 0, ""}
    };

    return 0;
}
